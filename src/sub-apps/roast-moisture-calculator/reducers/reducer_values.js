import {} from '../contants';

const initialState = {
  fcTime: '0:00';,
  totalTime: '0:00'
}

export default function values(state = initialState, action) {
  switch(action.type) {
    default:
    return state;
  }
}
